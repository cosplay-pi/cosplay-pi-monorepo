{
  "name": "costume-chip-hub-backend",
  "version": "0.0.1",
  "scripts": {
    "build": "run -T tsc",
    "run-dev": "node dist/main.js",
    "prisma-db-push": "prisma db push",
    "prisma-studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^4.0.0",
    "costume-chip-device-service-protocol": "workspace:*",
    "express": "^4.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.0.0",
    "prisma": "^4.0.0"
  },
  "nx": {
    "targets": {
      "run-dev": {
        "dependsOn": [
          "build"
        ]
      }
    }
  }
}
